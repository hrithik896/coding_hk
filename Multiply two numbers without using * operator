//Multiply two numbers without using * operator
#include<stdio.h>
int main(void)
{
    int a,b,i,result=0;
    printf("Enter the number:\n");
    scanf("%d%d",&a,&b);
    for(i=1;i<=b;i++)
       result=result+a;
    printf("multiply=%d",result);
  return 0;
}
